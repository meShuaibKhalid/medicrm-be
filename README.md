# Medical Store Backend

## Setup

1. `npm install`
2. `cp .env.example .env`
3. Set `MONGO_URI` and `JWT_SECRET`
4. `npm run seed:categories`
5. `npm run seed:products`
6. `npm run dev`

Default local Mongo example:

`MONGO_URI=mongodb://localhost:27017/medi-crm`
